# Hardware Configuration Template
# This file is generated by nixos-generate-config
# Copy this to hardware-configuration.nix and customize for your system
#
# To generate your actual hardware configuration, run:
#   sudo nixos-generate-config --show-hardware-config

{ config, lib, pkgs, modulesPath, ... }:

{
  imports = [
    (modulesPath + "/installer/scan/not-detected.nix")
  ];

  # EXAMPLE: Boot configuration
  # Uncomment and modify for your system
  # boot.initrd.availableKernelModules = [ "xhci_pci" "ahci" "nvme" "usb_storage" "sd_mod" "rtsx_pci_sdmmc" ];
  # boot.initrd.kernelModules = [ ];
  # boot.kernelModules = [ "kvm-intel" ];  # or "kvm-amd" for AMD
  # boot.extraModulePackages = [ ];

  # EXAMPLE: Filesystem configuration
  # Replace UUIDs with your actual partition UUIDs
  # Find them with: lsblk -f or blkid
  #
  # fileSystems."/" = {
  #   device = "/dev/disk/by-uuid/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX";
  #   fsType = "ext4";
  # };
  #
  # fileSystems."/boot" = {
  #   device = "/dev/disk/by-uuid/XXXX-XXXX";
  #   fsType = "vfat";
  # };
  #
  # swapDevices = [
  #   { device = "/dev/disk/by-uuid/XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX"; }
  # ];

  # EXAMPLE: Network interfaces
  # networking.useDHCP = lib.mkDefault true;
  # networking.interfaces.enp0s31f6.useDHCP = lib.mkDefault true;
  # networking.interfaces.wlp4s0.useDHCP = lib.mkDefault true;

  # EXAMPLE: CPU microcode
  # hardware.cpu.intel.updateMicrocode = lib.mkDefault config.hardware.enableRedistributableFirmware;
  # hardware.cpu.amd.updateMicrocode = lib.mkDefault config.hardware.enableRedistributableFirmware;

  # EXAMPLE: Enable firmware
  # hardware.enableRedistributableFirmware = true;

  # This value determines the NixOS release from which the default
  # settings for stateful data were taken.
  nixpkgs.hostPlatform = lib.mkDefault "x86_64-linux";
}
